file.edit("index.Rmd")
dir()
library(tidyverse)
read_delim("Unterrichtsunterlagen_HS19/13_Statistik1/data/novanimal.csv")
read_delim("Unterrichtsunterlagen_HS19/13_Statistik1/data/novanimal.csv", delim = ";")
df <- read_delim("Unterrichtsunterlagen_HS19/13_Statistik1/data/novanimal.csv", delim = ";")
group_by(df, gender, member) %>% summarise()
group_by(df, gender, member) %>% summarise(tot = n)
group_by(df, gender, member) %>% summarise(tot = n())
719 +816 +345 +339
getwd()
knitr::opts_chunk$set(fig.width = 20, fig.height = 12, warning = F, message = F, error = F, include = T, fig.pos = 'H')
canteen <- group_by(nova, gender, member) %>%
summarise(tot = n()) %>%
ungroup() %>%
mutate(canteen_member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"))
library(dyplr)
library(tidyverse)
canteen <- group_by(nova, gender, member) %>%
summarise(tot = n()) %>%
ungroup() %>%
mutate(canteen_member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"))
nova <- read_delim("13_Statistik1/data/novanimal.csv", delim = ";")
canteen <- group_by(nova, gender, member) %>%
summarise(tot = n()) %>%
ungroup() %>%
mutate(canteen_member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"))
population_exp <- c(345, 719, 339, 816) # Achtung: reihenfolge muss stimmten vgl. canteen_member
chi_sq <- chisq.test(canteen$tot, population_exp)
chi_sq
isher_t <- tibble(
member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"),
population = round(population_exp * 2138,0),
stichprobe = canteen$tot,
population_pct = population_exp,
canteen_pct = canteen$tot / sum(canteen$tot))
isher_t
fisher_t <- tibble(
member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"),
population = round(population_exp),
stichprobe = canteen$tot,
population_pct = population_exp,
canteen_pct = canteen$tot / sum(canteen$tot))
fisher_t
fisher_t <- tibble(
member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"),
stichprobe = canteen$tot,
population_pct = population_exp,
canteen_pct = canteen$tot / sum(canteen$tot))
fisher_t <- tibble(
member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"),
stichprobe = canteen$tot,
gesamtheit = gesamtheit_exp,
stichprobe_pct = gesamtheit / sum(gesamtheit),
canteen_pct = canteen$tot / sum(canteen$tot)) # berechne prozentsätze für stichprobe und population
gesamtheit_exp <- c(345, 719, 339, 816) # Achtung: reihenfolge muss stimmten vgl. canteen_member
fisher_t <- tibble(
member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"),
stichprobe = canteen$tot,
gesamtheit = gesamtheit_exp,
stichprobe_pct = gesamtheit / sum(gesamtheit),
canteen_pct = canteen$tot / sum(canteen$tot)) # berechne prozentsätze für stichprobe und population
fisher_t
fisher_t
group_by(nova, gender, member) %>%
summarise(tot = n()) %>%
ungroup()
fish <- fisher.test(fisher_t[ ,2:3], simulate.p.value=TRUE, B = 100000)
fish
fisher_t[ ,2:3]
c(canteen$tot, population_exp)
# bereitet eure Daten auf;
# tibble() ist eine ähnliche Funktion wie data.frame()
df_t <- tibble(
member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"),
stichprobe = canteen$tot,
gesamtheit = gesamtheit_exp,
stichprobe_pct = gesamtheit / sum(gesamtheit),
canteen_pct = canteen$tot / sum(canteen$tot)) # berechne prozentsätze für stichprobe und population
canteen <- group_by(nova, gender, member) %>%
summarise(tot = n()) %>%
ungroup() %>%
mutate(canteen_member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"))
canteen
canteen <- group_by(nova, gender, member) %>%
summarise(stichprobe = n()) %>%
ungroup() %>%
mutate(canteen_member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"),
gesamtheit = c(345, 719, 339, 816))
canteen
df_t <- group_by(nova, gender, member) %>%
summarise(stichprobe = n()) %>%
ungroup() %>%
mutate(canteen_member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"),
gesamtheit = c(345, 719, 339, 816), # Achtung: reihenfolge muss stimmten vgl. canteen_member
stichprobe_pct = gesamtheit / sum(gesamtheit),
canteen_pct = canteen$tot / sum(canteen$tot))
group_by(nova, gender, member) %>%
summarise(stichprobe = n()) %>%
ungroup() %>%
mutate(canteen_member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"),
gesamtheit = c(345, 719, 339, 816), # Achtung: reihenfolge muss stimmten vgl. canteen_member
stichprobe_pct = gesamtheit / sum(gesamtheit),
canteen_pct = canteen$tot / sum(canteen$tot))
group_by(nova, gender, member) %>%
summarise(stichprobe = n())
group_by(nova, gender, member) %>%
summarise(stichprobe = n()) %>%
ungroup() %>%
mutate(canteen_member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"),
gesamtheit = c(345, 719, 339, 816), # Achtung: reihenfolge muss stimmten vgl. canteen_member
stichprobe_pct = gesamtheit / sum(gesamtheit)
)
df_t <- group_by(nova, gender, member) %>%
summarise(stichprobe = n()) %>%
ungroup() %>%
mutate(canteen_member = c("Mitarbeiterinnen", "Studentinnen", "Mitarbeiter", "Studenten"),
gesamtheit = c(345, 719, 339, 816), # Achtung: reihenfolge muss stimmten vgl. canteen_member
stichprobe_pct = gesamtheit / sum(gesamtheit),
canteen_pct = stichprobe / sum(stichprobe))
df_t
# berechnet den Chi-Quadrat-Test
chi_sq <- chisq.test(df_t$stichprobe, df_t$gesamtheit)
chi_sq
df_t[ ,2:3]
df_t[ ,3:4]
df_t[ ,3:5]
df_t[ ,c(3,5)]
fish <- fisher.test(df_t[ ,c("stichprobe", "gesamtheit"], simulate.p.value=TRUE, B = 100000)
df_t[ ,c("stichprobe", "gesamtheit")]
fish <- fisher.test(df_t[ ,c("stichprobe", "gesamtheit")], simulate.p.value=TRUE, B = 100000)
fish
# berechnet den Chi-Quadrat-Test
chi_sq <- chisq.test(df_t[ ,c("stichprobe", "gesamtheit")])
chi_sq
